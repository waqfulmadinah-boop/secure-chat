services:
  - type: web
    name: secure-chat
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: JWT_SECRET
        sync: false
      - key: ALLOWED_EMAILS
        sync: false
      - key: PORT
        value: 3000
